spring:
  config:
    import:
      - "application-swagger.yml"
  profiles:
    active: ${spring.profiles.active}
    group:
      local:
        - db
      test:
        - db



server:
  port: 8099

